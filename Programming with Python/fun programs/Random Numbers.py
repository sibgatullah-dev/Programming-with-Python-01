# we will use two methods random() and randint(start,end)
import random
print(random.random())# computer will take any random number from 0 to 1
print(random.random())# computer will take any random number from 0 to 1

print(random.randint(1,20))# randint allows us to take a random number according to our preference 
print(random.randint(1,20))# randint allows us to take a random number according to our preference 