# if we want to delet an element from a lsit we can use del() function
li = [1,2,3,4,5,6,7,8,9]
del(li[4])# 5 will get deleted , since del() is a function not a method it calls like this not li.del() which is how we call methods 
print(li)